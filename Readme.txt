FIrst build the jar file using maven , it will go to target folder inside local repo
then use that jar file name in the DOCKERFILE 


 To build image run below command
 -- docker build -t springboot22(this name is can  be changes) .

 To run the application
 -- docker-compose up --build

